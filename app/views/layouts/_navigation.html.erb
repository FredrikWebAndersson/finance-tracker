<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "FinanceTracker", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <% if current_user %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item <%= "active" if request.path == my_portfolio_path %>">
            <%= link_to "My Portfolio", my_portfolio_path, class: "nav-link" %>
          </li>
          <li class="nav-item <%= "active" if request.path == "#" %>">
            <%= link_to "Friends", friends_path, class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Your Profile
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                <%= fa_icon "edit" %> Edit Profile
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
        </ul> 
      <% end %>
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <li class="nav-item disable user-name mr-3">
            <%= fa_icon "user" %> <%= current_user.full_name %>
          </li>
        <% end %>
        <li class="nav-item">
          <% if !user_signed_in? %>
            <%= link_to "Login", new_user_session_path, class: "session-btn btn btn-success my-2 my-lg-0 mr-2" %>
            <%= link_to "Signup", new_user_registration_path, class: "session-btn btn btn-success my-2 my-lg-0 mr-2" %>
          <% else %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "session-btn btn btn-success my-2 my-lg-0 mr-2" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div id="page-content" class="container pt-5 pb-5">